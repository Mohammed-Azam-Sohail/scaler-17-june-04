name: scaler-4

on: [push]

jobs:
  use-secret:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: secret usage
        run: echo 'The secret is \${{ secrets.SECRET }}'
